//SPDX-License-Identifier: Unlicense
pragma solidity ^0.8.4;

import "hardhat/console.sol";


contract Password {
  
  function setPassword(string memory _tag, string memory _password) public{
    // console.log("set password for tag '%s' to '%s'", _tag, _password);
  }
}
